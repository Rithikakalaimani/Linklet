// Cache TTL in seconds (24 hours)
const CACHE_TTL = 24 * 60 * 60;

// Default URL expiration (30 days)
const DEFAULT_EXPIRATION_DAYS = 30;

// Short code length
const SHORT_CODE_LENGTH = 7;

module.exports = {
  CACHE_TTL,
  DEFAULT_EXPIRATION_DAYS,
  SHORT_CODE_LENGTH,
};
